import React from 'react';
import { SideContent } from '.';
import { SideTab } from '.';

export const SideBar = () => {
    return (
        <div>
            <SideTab />
            <SideContent />
        </div>
    )
};

